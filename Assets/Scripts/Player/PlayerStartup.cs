using Bolt;

/// <summary> 
/// Enables the Player Controller based on ownership.
/// </summary> 
/*
namespace Player
{
	public class PlayerStartup : EntityBehaviour<INewPlayerState> 
	{
		public PlayerState playerState;
		public PlayerController controller;
		public HUDRelay playerHUD;

		public override void Attached () 
		{
			controller.enabled = playerState.IsMine;




			UnityEngine.Debug.LogError("Attached: PlayerStartup" + playerState.IsMine);
			if (playerState.IsMine)
			{
				

				if (playerState.IsMine)
					playerHUD.Initialize();
			}*/
						

			//state.SetTransforms(state.Transform, transform);

			//state.Transform.ChangeTransform(state.Transform, transform);
/*

		}
	}
}*/